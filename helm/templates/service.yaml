apiVersion: v1
kind: Service
metadata:
  name: {{ include "roadmap-dashboard.fullname" . }}
  labels:
    {{- include "roadmap-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "roadmap-dashboard.selectorLabels" . | nindent 4 }}
